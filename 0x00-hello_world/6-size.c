#include <stdio.h>

/**
 * main - This program prints the size of various types on my computer
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
sizeof(int);
sizeof(char);
sizeof(long int);
sizeof(long long int);
sizeof(float);
sizeof(double);
return (0);
}
